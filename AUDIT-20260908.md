# 前后端审查报告

审查日期：2026-09-08。范围：本地 Express/EJS 网站、前后台脚本、sql.js 数据持久化、部署脚本。未连接线上服务器，以下不代表线上配置已经核验。审查未修改业务代码；所有写入及安全复现均在 `.audit-review` 副本完成。

## P1：优先修复

1. **后台列表存在存储型 XSS。** `views/admin/news.ejs:170–174` 把接口标题直接拼进 innerHTML，轮播、项目等列表也有同类写法。隔离副本中新建一条含无害 onerror 标记的隐藏新闻，打开新闻管理页后标记成功执行。攻击者须能影响被保存的内容；执行位置是管理员同源页面，可读取 localStorage 中的令牌并调用接口。修复：通过 textContent/DOM API 渲染文本，正确处理属性与 URL，上下文转义；逐一检查所有后台列表。只清洗文章正文不足以修复标题注入。

2. **更新流程会删除真实内容并写回演示数据。** `deploy/update-from-windows.sh:80` 自动执行 `database/seed-nav-content.js`；后者第84、93、103、113、122、141行删除平台、专利、论文、媒体、学生及数据集/教材记录。即使不覆盖 lab.db 文件，也会覆盖其业务内容。修复：从更新流程中移除 seed，改为带版本号的增量迁移，演示数据仅用于明确的空白开发环境。

3. **改密和退出不能撤销旧 JWT。** `routes/api.js:158` 只验证签名与过期时间；263行仅销毁 session，297行仅更新密码。隔离测试中改密成功后，旧令牌请求设置接口仍返回200；退出后同样可使用旧 Bearer token。令牌有效期24小时。修复：检查用户状态和 tokenVersion，改密递增版本；退出撤销当前会话或令牌。

4. **当前本地数据仍可用默认管理员密码登录。** `database/init.js:176` 固定创建默认密码；隔离副本的登录测试确认默认账号有效。不能据此认定线上也相同，但若直接部署当前数据库，就会携带该账号。修复：初始化要求设置独立密码，生产启动检测默认口令，首次登录强制改密。报告不记录令牌或密码散列。

## P2：功能和可靠性

5. **一键更新脚本会在远程更新前退出。** `deploy/update-from-windows.sh:62` 调用未定义的 warn，而脚本启用 set -e；本文件只定义 step、ok、die。上传结束后会因 command not found 中断，ssh 更新段不会运行。修复时必须同时处理第2项，不能只补 warn 后直接执行。

6. **数据库直接覆盖写入，存在损坏与多进程丢更新风险。** `database/db.js:158,212` 将整个内存数据库直接 writeFileSync 到 lab.db。写入中断可能留下截断文件；进程内 Promise 锁不能协调两个 Node 进程，后写入的旧内存快照可以覆盖其他进程修改。修复：优先原生 SQLite 事务；至少临时文件写入、原子替换、备份与单写进程约束。本次为静态确认，未进行断电或破坏性测试。

7. **首页“更多动态”进入通知列表，不能查看完整新闻。** `views/index.ejs:23` 指向 /notices；`routes/index.js` 的对应处理只查询 notices，而首页只取前20条 news。新闻数量增加后，旧新闻缺少正常列表入口。修复：提供分页新闻列表并修正链接。

8. **后台设置保存成功，但部分前台仍显示硬编码内容。** `views/admin/settings.ejs:161–168` 保存副标题、邮箱、电话、地址、页脚文字；前台 `views/partials/footer.ejs:20–24`、`views/about.ejs:150` 等未读取这些配置。修复：前台统一绑定设置值，缺失时才用默认值，并验证保存后页面实际变化。

9. **演示媒体内容被作为正式报道启用。** 当前数据库实查有8条 is_active=1 的媒体记录指向 https://example.com/；`database/seed-nav-content.js:40` 起为这些演示报道定义。首页会把它们当作正式报道展示，点击也不能到达报道原文。其他 seed 中的论文、专利、学生同样被明确标为演示数据，需由实验室逐项核对，不能当作已验证成果。修复：下架示例记录，使用经确认的真实内容和来源。

10. **静态资源缓存没有版本化，上线可能继续显示旧布局。** `deploy/nginx-lab-website.conf:27–28` 缓存 CSS/JS 7天，而 header/footer 引用稳定文件名，没有内容哈希或版本参数。老访客可能拿到新HTML与旧CSS/JS组合。修复：资源文件名加内容哈希，或至少统一发布版本参数；部署检查纳入缓存命中场景。

## 验证结果和限制

- 隔离副本运行 npm test：冒烟测试和浏览器测试通过，涵盖页面、搜索、登录、新闻/通知发布、预览及详情。
- 额外验证：后台脚本注入成功；改密后旧token仍返回200；默认登录有效；8条示例媒体记录启用。
- 现有测试不能据此证明安全：未覆盖标题注入、token撤销、部署脚本、设置与前台一致性、历史新闻入口、缓存版本问题。
- 本轮未执行生产更新、未运行破坏性seed、未连接外部服务器或核验真实学术成果。依赖漏洞公告扫描不在本轮已完成验证范围。
- 建议顺序：先处理1–4，再同时处理5及安全迁移；随后修复数据持久化、前后台功能一致性与发布缓存。
