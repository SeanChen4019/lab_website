# 实验室网站 · 云服务器傻瓜部署教程

> 适用环境：**全新的 Ubuntu 22.04 / 24.04 云服务器**
> 网站类型：Node.js + Express + EJS 服务端渲染，SQLite（sql.js）存数据
> 目标：从一台纯净服务器，一路做到 **域名 + HTTPS 正式上线**
>
> 阅读方式：**从上往下，一步一步照抄命令**。本教程已填入你的真实信息：
> - 服务器公网 IP：**121.199.37.30**
> - 域名：**seanapi.com**（同时支持 www.seanapi.com）
>
> 剩下写着 `【改我】` 的地方（如 SSH 用户名/密码），替换成你自己的值再执行。

---

## 0. 开始前，先把这几样准备好

| 需要的东西 | 说明 | 你的值 |
|---|---|---|
| 服务器公网 IP | 云厂商控制台能看到 | `121.199.37.30` |
| SSH 登录用户名 | 一般是 `root` 或 `ubuntu` | `【改我】` |
| SSH 登录密码 / 密钥 | 登录服务器用 | `【改我】` |
| 你的域名 | 主域名 | `seanapi.com` |
| 域名解析平台 | 已决定用腾讯云 DNSPod | 见下方「DNS 说明」 |

### DNS 说明（重要，先看懂再动手）

`seanapi.com` 在**腾讯云**注册，但它的 DNS 服务器（NS）原本指向 **Cloudflare**。要让解析生效，已经决定**切换到腾讯云 DNSPod**，操作如下：

1. 腾讯云域名管理里，若开了 **DNSSEC 先关掉**（否则改 NS 会失败）。
2. 在解析面板的提示弹窗点 **「一键修改」**，把 NS 改成：
   ```
   fuchsia.dnspod.net
   clarence.dnspod.net
   ```
3. 等 NS 生效（10 分钟 ~ 48 小时），点「已经修改，重新验证」，红色警告消失即成功。
4. 确认这两条 A 记录存在，记录值都是 `121.199.37.30`：
   | 主机记录 | 类型 | 记录值 |
   |---|---|---|
   | `@` | A | 121.199.37.30 |
   | `www` | A | 121.199.37.30 |
5. 验证：`ping seanapi.com` 和 `ping www.seanapi.com` 都返回 `121.199.37.30` 才算通。

> ⚠️ 切换后 Cloudflare 那边所有解析设置全部作废。若在 Cloudflare 配过邮箱（MX）或其他子域名，需在 DNSPod 里补齐，否则那些服务会断。
> **必须等 DNS 生效（第 5 条 ping 通）后，才能做下面的第 4、5 步。**

**部署包已经打好了，在这里：**

```
dist\lab-website-ubuntu-20260903.tar.gz
```

SHA256 校验和（用于核对文件是否传输完整）：

```
22e607743dd95ac6293bb8b902cf1e89d5ef1585b08e8870c3322c7b61274efe
```

> 整个流程分 7 步，预计 20~30 分钟。**第 1 步在本地 Windows 做，第 2 步之后全部在服务器上 SSH 里做。**
>
> 💡 **嫌麻烦？直接用下面的「一键部署」，一条命令搞定，第 1~7 步可全部跳过。** 手动分步教程保留在后面，供理解原理或排查问题时参考。

---

## 🚀 一键部署（推荐，最省事）

已经为你写好两个脚本，把「装环境 → 依赖 → 服务 → Nginx → HTTPS → 防火墙」全部自动化：

| 脚本 | 在哪运行 | 作用 |
|---|---|---|
| `deploy/deploy-from-windows.sh` | 你自己的 Windows（Git Bash） | 打包 + 上传 + 远程触发部署，**全程一条命令** |
| `deploy/setup-all-in-one.sh` | 服务器上 | 真正干活的一键部署脚本（也被上面那个自动调用） |

### 方式 A：从 Windows 一条命令全自动（最傻瓜）

**1.** 打开 `deploy/deploy-from-windows.sh`，改顶部【配置区】4 个值（已按你的信息预填好）：
```bash
SERVER_USER="root"                 # SSH 用户名
SERVER_IP="121.199.37.30"          # 服务器 IP
DOMAIN="seanapi.com"               # 域名
EMAIL="2415483828@qq.com"          # 证书邮箱
```

**2.** 在 Windows 的 **Git Bash** 里运行：
```bash
cd "E:/平台文件/实验室网站/lab-website"
bash deploy/deploy-from-windows.sh
```

**3.** 过程中会**两次提示输入服务器密码**（上传一次、登录一次），输入即可（屏幕不显示是正常的）。然后等 5~15 分钟，看到「部署完成」就成功了。

> 脚本会自动：用最新代码打包 → scp 上传 → ssh 登录 → 解压 → 运行服务器端一键脚本 → 清理临时包。

### 方式 B：已经在服务器上，直接跑一键脚本

如果你已经把部署包上传并解压到了 `/opt/lab-website/current`（或想用阿里云网页版 Workbench 终端），直接：
```bash
cd /opt/lab-website/current
sudo bash deploy/setup-all-in-one.sh seanapi.com 2415483828@qq.com
```
一条命令做完全部 9 个步骤，最后打印访问地址。

### 一键脚本会自动处理这些「坑」

- ✅ **npm 慢** → 自动切国内镜像 `registry.npmmirror.com`（坑 2）
- ✅ **Node 版本** → 自动检测，低于 20 就用 NodeSource 装 Node 20
- ✅ **密钥** → 自动生成随机 SESSION/JWT 密钥（已存在则保留不覆盖）
- ✅ **域名** → 自动写进 Nginx 的 `server_name`
- ✅ **HTTPS** → 自动 certbot 签发 + 开启 HTTP→HTTPS 跳转 + 自动续期
- ✅ **防火墙** → 自动放行 SSH/HTTP/HTTPS
- ✅ **幂等** → 失败后可反复重跑，不会破坏数据

### ⚠️ 一键脚本唯一搞不定的事：阿里云安全组

安全组在**云控制台**，脚本在服务器内部**无权修改**。所以如果最后 certbot 那步报超时失败，**99% 是安全组没放行 80/443**（见文末「实战踩坑记录 · 坑 3」）。去控制台加两条入方向规则（80、443，来源 `0.0.0.0/0`），然后重跑方式 B 即可补上 HTTPS。

---

## 第 1 步 · 本地：把部署包上传到服务器

**在哪做：** 你自己的 Windows 电脑，打开 **Git Bash**（不是 cmd，不是 PowerShell）。

```bash
cd "E:/平台文件/实验室网站/lab-website/dist"

# 把 【改我】 换成你的 SSH 用户名，IP 已填好
scp lab-website-ubuntu-20260903.tar.gz 【改我】用户名@121.199.37.30:~/
```

- 第一次连会问 `Are you sure you want to continue connecting?` → 输入 `yes` 回车。
- 然后输入服务器密码（**输入时屏幕不显示，是正常的**），回车。
- 看到进度条走到 100% 就成功了。

✅ **完成标志：** 文件传完没报错。

---

## 第 2 步 · 服务器：登录并安装基础环境

**在哪做：** SSH 登录到服务器。

```bash
ssh 【改我】用户名@121.199.37.30
```

登录后，依次执行下面命令，安装 Nginx、OpenSSL 和 Node.js 20：

```bash
sudo apt update
sudo apt install -y curl nginx openssl

# 添加 Node.js 20 官方源并安装
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

装完后**必须验证**这 4 条：

```bash
node -v        # 期望：v20.x.x
npm -v         # 期望：10.x.x
nginx -v       # 期望：nginx version: nginx/1.x
which node     # 期望：/usr/bin/node   ← 必须是这个路径！
```

> ⚠️ **重点检查 `which node`：** 结果必须是 `/usr/bin/node`。
> 如果不是（比如用了 nvm 装成了 `/home/xxx/.nvm/...`），后面的 systemd 服务会启动失败。
> 解决：卸载 nvm 版 node，严格按上面 NodeSource 的方式重装。

✅ **完成标志：** 4 条命令都符合期望，尤其 `which node` = `/usr/bin/node`。

---

## 第 3 步 · 服务器：解压 + 一键自动部署

继续 SSH 在服务器上执行：

```bash
# （可选）核对文件完整性，输出应等于第 0 步的 SHA256
sha256sum ~/lab-website-ubuntu-20260903.tar.gz

# 解压到指定目录
sudo mkdir -p /opt/lab-website/current
sudo tar -xzf ~/lab-website-ubuntu-20260903.tar.gz -C /opt/lab-website/current

# 进入目录并运行自动部署脚本
cd /opt/lab-website/current
sudo bash deploy/setup-ubuntu.sh
```

**这个脚本会自动帮你做这些事（不用手动操作）：**

1. 创建一个没有登录权限的系统用户 `labwebsite`（更安全）
2. 安装生产依赖（`npm ci --omit=dev`）
3. 用 openssl 自动生成随机的 Session 密钥和 JWT 密钥
4. 跑一遍生产自检（检查关键文件和数据库表是否齐全）
5. 注册并启动 systemd 服务（开机自启、崩溃自动重启）
6. 安装 Nginx 反向代理配置

脚本跑完会打印「部署完成」。然后**本机验证**：

```bash
curl http://127.0.0.1/healthz
```

✅ **完成标志：** 返回 `{"status":"ok"}`。
此时用浏览器打开 `http://121.199.37.30/` 也应该能看到网站了（还没上 HTTPS）。

---

## 第 4 步 · 服务器：绑定你的域名 seanapi.com

> 前提：第 0 步「DNS 说明」里的解析已经生效（`ping seanapi.com` 返回 121.199.37.30）。没生效就先别做这步。

### 4.1 确认解析已生效

在服务器上验证（`Ctrl+C` 停止 ping）：

```bash
ping seanapi.com
ping www.seanapi.com
```

✅ 两个都返回 `121.199.37.30` 才继续。

### 4.2 修改 Nginx 配置里的域名

```bash
sudo nano /etc/nginx/sites-available/lab-website
```

找到这一行：

```nginx
server_name _;
```

改成你的域名（裸域名 + www 一起写，空格隔开）：

```nginx
server_name seanapi.com www.seanapi.com;
```

改完保存：按 `Ctrl+O` → 回车 → `Ctrl+X`。

测试并重载 Nginx：

```bash
sudo nginx -t && sudo systemctl reload nginx
```

✅ **完成标志：** `nginx -t` 显示 `syntax is ok` 和 `test is successful`。

---

## 第 5 步 · 服务器：申请免费 HTTPS 证书

用 Let's Encrypt（免费、自动续期）：

```bash
sudo apt install -y certbot python3-certbot-nginx

# 一条命令同时给裸域名和 www 签证书
sudo certbot --nginx -d seanapi.com -d www.seanapi.com
```

按提示操作：

1. 输入邮箱（用于证书到期提醒）→ 回车
2. 同意条款 → 输入 `Y` 回车
3. 是否分享邮箱给 EFF → 输入 `N` 回车（随意）
4. 是否把 HTTP 自动跳转到 HTTPS → **选 `2`（Redirect）**，推荐

certbot 会自动改写 Nginx 配置、加上 443 端口和 SSL、并配好自动续期。

验证自动续期是否正常：

```bash
sudo certbot renew --dry-run
```

✅ **完成标志：** dry-run 显示 `Congratulations, all simulated renewals succeeded`。
此时浏览器打开 `https://seanapi.com` 应显示小锁图标，网站正常。

> 说明：应用里 `COOKIE_SECURE=auto` + Nginx 已传 `X-Forwarded-Proto`，所以 HTTPS 下后台登录 Cookie 会自动走安全模式，**不用改任何代码**。

---

## 第 6 步 · 服务器：配置防火墙（只放行必要端口）

```bash
sudo ufw allow OpenSSH
sudo ufw allow 'Nginx Full'    # 同时放行 80 和 443
sudo ufw enable                # 问是否继续，输入 y 回车
sudo ufw status                # 查看已放行规则
```

✅ **完成标志：** `ufw status` 里能看到 OpenSSH 和 Nginx Full 都是 `ALLOW`。

> Node 应用只监听本机 `127.0.0.1:3000`，不对公网开放，所以 **3000 端口不需要放行**。

---

## 第 7 步 · 上线后必做的两件事 ⚠️

### 7.1 立刻修改后台默认管理员密码

浏览器打开：

```
https://seanapi.com/admin
```

登录后马上修改默认密码。

### 7.2 确认服务正在运行且开机自启

```bash
sudo systemctl status lab-website
```

✅ **完成标志：** 显示 `active (running)`，且 `enabled`。按 `q` 退出查看。

🎉 **到这里，网站已经正式上线！**

---

## 日常维护命令速查

```bash
# 看网站实时日志（排查问题时最常用，Ctrl+C 退出）
sudo journalctl -u lab-website -f

# 重启网站服务（改了代码或配置后）
sudo systemctl restart lab-website

# 查看网站服务状态
sudo systemctl status lab-website

# 改了 Nginx 配置后，先测试再重载
sudo nginx -t && sudo systemctl reload nginx
```

---

## 数据备份（重要！）

网站会变化的数据**只有两处**：数据库文件 + 后台上传的文件。升级或迁移前必须一起备份：

```bash
sudo tar -czf ~/lab-website-data-$(date +%F).tar.gz \
  /opt/lab-website/current/database/lab.db \
  /opt/lab-website/current/public/uploads
```

备份包会生成在你的家目录，名字带当天日期。建议再 `scp` 下载到本地保存。

---

## 以后如何更新网站内容

- **改文字/图片/新闻等**：直接登录后台 `https://你的域名/admin` 在线编辑，**不用重新部署**。
- **改了代码要重新部署**：在本地重新打包上传，覆盖 `/opt/lab-website/current`，然后 `sudo systemctl restart lab-website`。重新部署前**先按上面做数据备份**，避免覆盖掉 `lab.db` 和 `uploads`。

---

## 常见问题排查（FAQ）

**Q1：`curl http://127.0.0.1/healthz` 报错 / 网页打不开**

```bash
sudo systemctl status lab-website      # 看服务是否 active
sudo journalctl -u lab-website -n 50   # 看最近 50 行日志找报错原因
```
最常见原因：`which node` 不是 `/usr/bin/node`（见第 2 步警告），导致 systemd 找不到 node。

**Q2：浏览器打开 IP 或域名是 502 Bad Gateway**

说明 Nginx 通了但后端 Node 没起来。同样用上面两条命令查 `lab-website` 服务日志。

**Q3：certbot 申请证书失败**

- 先确认第 4.1 步域名解析**已生效**（`ping 域名` 能到服务器 IP）。
- 确认防火墙/云厂商**安全组**放行了 80 端口。
- 确认 `server_name` 已改成你的域名（第 4.2 步）。

**Q4：域名能访问但云服务器安全组拦截**

除了服务器内的 `ufw`，**云厂商控制台还有一层「安全组」**。去控制台确保安全组入方向放行了 `22（SSH）、80（HTTP）、443（HTTPS）`。

**Q5：访问网站能通，但后台登录后又跳回登录页**

多半是 HTTPS/HTTP 混用导致 Cookie 失效。确认已经通过 `https://` 访问，且第 5 步 certbot 已成功配置。

---

## 实战踩坑记录（本次部署真实遇到的问题）

> 这一节记录 2026-09-03 首次部署 `seanapi.com` 时实际踩到的坑，按发生顺序排列，供以后复现或排查参考。

### 坑 1 · 解析记录加了却不生效（DNS 服务器 / NS 指错平台）

**现象：** 在腾讯云 DNSPod 面板里加好了 `@` 和 `www` 两条 A 记录指向 `121.199.37.30`，但面板顶部一直有红色警告：「当前域名未设置正确的 DNS 服务器，DNS 解析服务未生效」。

**原因：** `seanapi.com` 虽在腾讯云注册，但它的 **NS（DNS 服务器）指向的是 Cloudflare**（`cass.ns.cloudflare.com` / `carlos.ns.cloudflare.com`）。全网查询域名时只去问 Cloudflare，根本不看腾讯云这边的记录 —— **记录加错了平台**。

**解决：** 在腾讯云解析面板的提示弹窗点 **「一键修改」**，把 NS 切换到 DNSPod：
```
fuchsia.dnspod.net
clarence.dnspod.net
```
等 NS 生效（10 分钟~48 小时）后，红色警告消失，A 记录才开始真正生效。
> 教训：**解析记录必须加在「NS 实际指向的那个平台」**。切换 NS 后，原 Cloudflare 上的邮箱(MX)/其他子域名记录都要在新平台补齐，否则那些服务会断。

### 坑 2 · `setup-ubuntu.sh` 里的 npm 安装巨慢 / 像卡死

**现象：** 运行部署脚本时停在安装依赖那步很久，刷一堆 `npm warn EBADENGINE`。

**原因：** 阿里云（国内）服务器默认从国外 npm 官方源 `registry.npmjs.org` 拉包，跨境极慢甚至假死。那些 `EBADENGINE` 只是无害警告，**不是慢的原因**。

**解决：** `Ctrl+C` 停掉，切换国内镜像后重跑（脚本幂等，可安全重跑）：
```bash
sudo npm config set registry https://registry.npmmirror.com --location=global
# 部署脚本实际用 labwebsite 用户跑 npm，给它也设一遍
sudo runuser -u labwebsite -- env HOME=/var/lib/labwebsite \
  npm config set registry https://registry.npmmirror.com
cd /opt/lab-website/current && sudo bash deploy/setup-ubuntu.sh
```
换镜像后依赖 1~3 分钟装完。

### 坑 3 · certbot 报 `Timeout during connect (likely firewall problem)`

**现象：** `sudo certbot --nginx -d seanapi.com -d www.seanapi.com` 失败，报错：
```
Detail: 121.199.37.30: Fetching http://seanapi.com/.well-known/acme-challenge/xxx:
Timeout during connect (likely firewall problem)
```
即 Let's Encrypt 的服务器从公网访问你的 80 端口做验证时超时。

**关键判断：超时（Timeout）≠ 拒绝（refused）**
- `Timeout`（卡住无响应）= 数据包被防火墙**丢弃**了 → 查防火墙/安全组。
- `Connection refused`（立刻拒绝）= 端口没人监听 → 查 Nginx 是否运行。
本次是 Timeout，所以方向锁定在「公网到 80 端口的链路被挡」。

**逐层排查流程（从内到外，一层层排除）：**

**① Nginx 服务本身是否在运行？**
```bash
sudo systemctl status nginx
```
预期 `active (running)`。→ 本次正常，排除。

**② Nginx 是否在监听 80 端口？**
```bash
sudo ss -tlnp | grep ':80'
```
预期看到 `0.0.0.0:80` 和 `[::]:80` 由 nginx 监听。→ 本次正常（IPv4+IPv6 都在听），排除。

**③ 服务器本机自访是否通？**
```bash
curl -I http://127.0.0.1/
```
预期返回 HTTP 响应。→ 说明 Nginx→Node 链路正常，问题在「外部进不来」。

**④ 服务器内部防火墙 ufw 是否挡了？**
```bash
sudo ufw status
```
- `inactive` → 没开，排除。
- `active` 且列表无 80/443 → 就是它挡的：`sudo ufw allow 'Nginx Full'`。

**⑤ 云厂商安全组是否放行 80/443？（本次真正的原因）**
阿里云 ECS 在云控制台还有一层「安全组」，**独立于服务器内部的 ufw**，在系统里用 `ss`/`ufw` 查不到。纯净实例默认常只开 22。
去控制台：ECS 实例 → 安全组 → 配置规则 → **入方向** → 添加：

| 端口范围 | 授权对象 | 协议 |
|---|---|---|
| `80/80` | `0.0.0.0/0` | TCP |
| `443/443` | `0.0.0.0/0` | TCP |

**⑥ 从外部实测公网是否真的通了（关键验证）**
在**你自己的电脑**（不是服务器）执行：
```bash
curl -I http://seanapi.com
```
返回 `HTTP/1.1 200 OK` 才说明公网到 80 已打通。

**解决后重跑：**
```bash
sudo certbot --nginx -d seanapi.com -d www.seanapi.com
sudo certbot renew --dry-run     # 验证自动续期正常
```

> 本次根因：**第 ⑤ 步阿里云安全组未放行 80/443**。前 4 层（Nginx、端口、本机自访、ufw）都正常，唯独公网进不来。

> ⚠️ **收尾安全项（务必做）：** 排查时若图省事加了「所有 TCP、端口 1~65535、来源 0.0.0.0/0」这种全开规则，上线后**必须删掉**，改成只放行 `22 / 80 / 443` 三个端口。全网开放所有端口风险极高（数据库、调试端口都会被扫描）。SSH 的 22 端口最好只对自己常用 IP 开放。

---

## ⛔ 绝对不要做的事

1. **不要**在服务器上运行 `npm run init-db` 或 `npm run seed` —— 会**清空/覆盖**你现有的内容数据。
2. **不要**把 `/etc/lab-website/lab-website.env`（里面有密钥）公开或发给别人。
3. **不要**把本地的 `node_modules` 上传到服务器 —— 部署脚本会在服务器上重新安装。
4. **不要**放行 3000 端口到公网 —— 它只该监听本机。

---

# 附录：更新已上线的网站（2026-09-08 实战记录）

> 场景：服务器上已经跑着一个旧版本网站，本地改好了新版本，要把新版「完完整整」替换上去。
> 这次更新涉及的新东西：6 项新导航、4 个新页面（平台/研究成果/资源服务 + 后台 4 个管理页）、全站字体配色调整、4 张新数据表。

## 一、命令是怎么分工的（先看懂这张表再动手）

| 命令 | 在哪敲 | 干什么 |
|---|---|---|
| `cd /e/平台文件/实验室网站/lab-website/dist` | 自己电脑 | 进入打包输出文件夹 |
| `scp 文件 root@服务器:路径` | 自己电脑 | 把文件通过网络传到服务器 |
| `ssh root@121.199.37.30` | 自己电脑 | 登录服务器，之后就在服务器上敲命令了 |
| `tar -czf / -xzf` | 都可以 | 打包（c）/解压（x）压缩包 |
| `systemctl stop/start/restart/status` | 服务器 | 控制网站这个「服务」的停/启/状态 |
| `chown -R` | 服务器 | 修改文件归属，让网站账号有权读写 |
| `curl 网址` | 都可以 | 命令行版浏览器，用来验证网站是否活着 |

## 二、完整更新流程（照抄即可）

### 阶段一：在自己电脑上（Git Bash）

```bash
cd /e/平台文件/实验室网站/lab-website
```
> 进入项目文件夹。`/e/...` 是 Git Bash 里写 Windows E 盘的方式。

```bash
cd dist && tar -czf lab-website-update.tar.gz --exclude='node_modules' --exclude='.git' server.js package.json package-lock.json DEPLOY-UBUNTU.md UPDATE-ALUMNI-UBUNTU.md database deploy public routes scripts services views && cd ..
```
> 一条很长的打包命令，拆开看：
> - `tar -czf 输出文件名 要打包的内容…`：c=创建压缩包，z=用 gzip 压缩，f=指定文件名
> - `--exclude='node_modules'`：**跳过**依赖文件夹（几百 MB，服务器上会重新装，不用传）
> - 最后那串空格分隔的是要打包的文件/文件夹清单
> - 开头 `cd dist` 是让包生成在 dist 文件夹里，结尾 `cd ..` 退回项目根目录

```bash
scp dist/lab-website-update.tar.gz root@121.199.37.30:/opt/lab-website/current/
```
> scp = 通过 SSH 传文件。格式：`scp 本地文件 用户名@IP:服务器上的目标路径`。
> 会提示输入服务器密码，**输入时屏幕不显示任何字符，是正常的**，输完回车。
> ✅ 看到 `100%` 就是传完了。
>
> ⚠️ 这次我们直接传到 `/opt/lab-website/current/`（网站目录）。**上传后不要删它**——上次更新就是解压完把包删了，导致第二次替换时找不到包，又重传了一遍。

### 阶段二：在服务器上（先 `ssh root@121.199.37.30` 登录）

**① 停网站**（替换数据库时网站必须停，不然会数据错乱）：

```bash
sudo systemctl stop lab-website
```
> `sudo` = 用管理员权限执行；`systemctl stop 服务名` = 停掉这个系统服务。
> 敲完没有任何提示就是成功（Linux 的规矩：没消息 = 没问题）。

**② 解压覆盖（完整体替换，连数据库一起换成本地的）**：

```bash
sudo tar -xzf /opt/lab-website/current/lab-website-ubuntu-20260908-143619.tar.gz -C /opt/lab-website/current
```
> 拆开看：
> - `tar -xzf 包名`：x=解压（extract），czf 的反向操作
> - `-C 目标目录`：解压到哪个文件夹（大写 C，注意）
>
> 💡 两种解压姿势的区别（这次更新学到的重点）：
> - **整包替换**（上面这条）：服务器数据库也换成你电脑里的版本。适合调试期。
> - **保留数据库替换**：在命令末尾加 `--exclude='database/lab.db' --exclude='public/uploads'`，服务器上后台发的通知/新闻、改过的密码都保留。适合网站已有真实内容之后。
>
> ✅ 没有输出、没有红色报错 = 成功。

**③ 把数据库和上传目录还给网站账号**：

```bash
sudo chown -R labwebsite:labwebsite /opt/lab-website/current/database /opt/lab-website/current/public/uploads
```
> 解压出来的文件归属是 root，但网站是以 `labwebsite` 这个受限账号运行的，
> 它需要对 `database`（存数据）和 `public/uploads`（存上传图片）有写权限。
> - `chown 用户:组 文件` = 改文件归属
> - `-R` = 连同里面所有文件一起改（递归）
> **漏了这条，网站能打开但后台保存会报错**——第一次更新就踩了这个坑。

**④ 重新启动网站**：

```bash
sudo systemctl start lab-website
```

**⑤ 验证网站活着**：

```bash
curl http://127.0.0.1:3000/healthz
```
> `curl` 是命令行版浏览器。`127.0.0.1` = 服务器自己，`:3000` = 网站程序监听的端口，
> `/healthz` = 网站自带的体检接口。
> ✅ 显示 `{"status":"ok"}` 就是成功。

**⑥ 删掉网站目录里的安装包**（可选，但建议；删了下次更新要重传，自己权衡）：

```bash
rm /opt/lab-website/current/lab-website-*.tar.gz
```

### 阶段三：回自己电脑的浏览器确认

按 **Ctrl + F5** 强制刷新（普通刷新可能读到缓存）。
不同浏览器的缓存是分开的：Chrome 正常 ≠ Edge 正常，两边都要刷。
Edge 若还是旧的，`Ctrl + Shift + Delete` 清除「缓存的图像和文件」。

## 三、这次更新踩过的坑（引以为戒）

1. **安装包删早了**：解压完就把 tar.gz 删了，结果发现数据库要重换，只能重新上传一遍。
   → 教训：网站完全确认正常之前，保留安装包和备份。
2. **Nginx 给 CSS/JS 设了 7 天缓存**：更新后浏览器可能还读旧样式，出现「Chrome 正常、Edge 乱码」。
   → 教训：更新后一律 Ctrl+F5；清缓存入口：`Ctrl+Shift+Delete`。
3. **服务器旧数据库里有一条乱码新闻**（早期后台发布测试留下的损坏数据），
   走「保留数据库」更新时它会继续显示在首页头条。
   → 教训：调试期图省事用整包替换最干净；网站有真实数据后改成保留式更新。
4. **解压后忘记 chown**：网站打开正常但后台保存会失败。
   → 教训：整包替换后，③ 那条 chown 命令永远要跟在后面。

## 四、出问题怎么回退（救命数据在第 ② 步前就有备份的话）

```bash
sudo systemctl stop lab-website
sudo tar -xzf ~/backup-*.tar.gz -C /opt/lab-website/current
sudo systemctl start lab-website
```
> `~/backup-*.tar.gz` 是更新前用下面这条命令做的备份（以后每次更新前都先做一次）：
> ```bash
> sudo tar -czf ~/backup-$(date +%F).tar.gz -C /opt/lab-website/current database/lab.db public/uploads
> ```
> 拆开看：`$(date +%F)` 会自动变成当天日期（如 2026-09-08），这样每份备份文件名不重复。

## 五、更新后必做的三件事

1. 后台密码若被整包替换重置回 `admin / admin123`，**立刻登录改掉**。
2. 首页 / 平台页 / 团队页 / 研究成果页 / 资源服务页逐个点一遍。
3. 后台「平台管理 / 专利管理 / 论文管理 / 媒体报道」里的内容目前是**演示数据**（专利号是编的），正式使用前替换成真实资料。
